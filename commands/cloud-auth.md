Authenticate to cloud providers. Use the cloud-auth-agent to handle provider selection and authentication.

Invoke the cloud-auth agent with: "Authenticate to cloud providers based on user selection."
